Goal:
Refactor the project to the following structure.

pages/
components/
styles/
js/
assets/images
assets/icons
design/
tasks/

Requirements:

- Move HTML pages to pages/
- Move reusable UI parts to components/
- Move CSS/SCSS to styles/
- Move JS to js/
- Move images and icons to assets/

Important:
Update all file references and import paths so the site works after refactoring.
