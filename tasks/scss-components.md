Refactor the styles using the scss component system

Every reusuable style should be stored in their respective variables using the scss component architecture

Style:

- Use Playfair Display font for the hero headline
- Use Playfair Display font for the hero highlight
- Use Inter font for the default text, including the hero text

Requirements:

- All the colors across the website should be stored in colors.scss and pulled from there
- All the spaces across the website should be stored in spaces.scss and used pulled there
- All the fonts across the website should be stored in typography.scss and used pulled there
